'use strict';

const radixReactContext = require('..');
const assert = require('assert').strict;

assert.strictEqual(radixReactContext(), 'Hello from radixReactContext');
console.info('radixReactContext tests passed');
