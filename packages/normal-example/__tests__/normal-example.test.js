'use strict';

const normalExample = require('..');
const assert = require('assert').strict;

assert.strictEqual(normalExample(), 'Hello from normalExample');
console.info('normalExample tests passed');
